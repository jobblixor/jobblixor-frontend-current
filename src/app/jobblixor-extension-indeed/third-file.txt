chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
    if (message.action === 'OPEN_EASY_APPLY_TAB' && message.url) {
        chrome.tabs.create({ url: message.url });
    }
});
